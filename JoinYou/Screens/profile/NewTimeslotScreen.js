import React from "react";
import { Text, View, StyleSheet } from "react-native";

const NewTimeslotScreen = () => {
  return (
    <View>
      <Text>NewTimeslotScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default NewTimeslotScreen;
